spring:
  application:
    name: employees

  datasource:
    url: jdbc:h2:file:./data/employee_db
    driverClassName: org.h2.Driver
    username: sorry
    password: mynameismethos

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console